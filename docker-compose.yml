services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: nenya
      POSTGRES_PASSWORD: nenya
      POSTGRES_DB: nenya
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
