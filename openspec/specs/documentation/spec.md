## ADDED Requirements

### Requirement: README documentation
The README SHALL document the full setup flow, MCP client configuration examples, REST API endpoint reference, and import instructions.

#### Scenario: New user setup
- **WHEN** a new user reads the README
- **THEN** they can clone, configure, run migrations, start the server, and connect a client without reading source code
